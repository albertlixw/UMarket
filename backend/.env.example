#replace <project-ref> with your project ID.
SUPABASE_URL=https://<project-ref>.supabase.co

# API key used to authenticate with supabase REST. Use the service role key for server side requests
SUPABASE_API_KEY=your-supabase-service-key

# JWT secret used to validate Supabase access tokens (Settings -> API -> JWT secret)
SUPABASE_JWT_SECRET=your-supabase-jwt-secret

# Column name used as the primary identifier for transactions (most setups should leave this as prod_id)
SUPABASE_TRANSACTION_ID_FIELD=prod_id

# Optional: override the PostgREST relationship name between Transactions and Product if you renamed the FK
# SUPABASE_PRODUCT_RELATION=Product!Transactions_prod_id_fkey

# Comma separated list of allowed frontend origins for CORS
FRONTEND_URLS=http://localhost:3000
